# Rust
/backend/target/

# Node
node_modules/
/frontend/dist/

# Generated blog artifacts — COMMITTED so Vercel can deploy without the Rust
# toolchain. Regenerate with `npm run bloggen` (or the `npm run blog` menu) and
# commit the result: /.gen/ and /frontend/public/blog-assets/ .
*.gen.json

# Copies staged into public/ by frontend/scripts/prepare-static.mjs at build
# time — the source of truth is profile.json + /.gen/ .
/frontend/public/profile.json
/frontend/public/blog-index.json
/frontend/public/blog/

# Lighthouse CI
/.lighthouseci/

# Playwright
/frontend/test-results/
/frontend/playwright-report/
/frontend/blob-report/
/frontend/.playwright/

# Env / editor
.env
.env.local
.DS_Store

# stray ts-rs output when TS_RS_EXPORT_DIR is unset
/backend/bindings/
